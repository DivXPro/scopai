# 技术债跟踪

## 当前已知技术债

### Worker 目标覆盖不完整

- `comment` 是当前主实现路径
- `post` 目标仍未完成 worker 支持
- `media` 路径需要结合真实数据进一步验证

### 设计稿与实现存在差异

- `docs` 中的早期规划文档包含比现状更理想化的 Bree / daemon 组织方式
- 后续更新时应继续把差异回写到文档，而不是隐含保留

### 测试自动化仍可增强

- 当前已有手工测试计划和报告
- 后续可增加更系统的自动化验证与文档联动
