# SECURITY.md

## 安全边界

- Anthropic API Key 通过配置文件或环境变量提供，不应写入仓库
- 导入的 JSONL 视为外部输入，处理前应假定其不可信
- 导出结果可能包含敏感内容，落盘路径应由操作者明确指定

## 当前建议

- 优先通过环境变量提供 `ANTHROPIC_API_KEY`
- 不要在文档中提交真实账号、真实 token 或生产数据路径
- 排查失败日志时，避免把完整敏感数据直接复制进长期文档
